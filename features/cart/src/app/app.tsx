import { Box, Typography } from '@mui/material';

export function App() {
  return (
    <Box p={2} sx={{ background: '#cceeff', textAlign: 'center' }}>
      <Typography variant='h4'>Cart</Typography>
    </Box>
  );
}

export default App;
